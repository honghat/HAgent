"""
Auto-generated PostgreSQL Schema translated from SQLite.
Generated by scripts/generate_pg_schema.py. Do not edit directly.
"""

SCHEMA_STATEMENTS = [
    "CREATE TABLE \"User\" (\n    \"id\" SERIAL NOT NULL PRIMARY KEY ,\n    \"name\" TEXT NOT NULL,\n    \"email\" TEXT NOT NULL,\n    \"password\" TEXT NOT NULL,\n    \"role\" TEXT NOT NULL DEFAULT 'user',\n    \"status\" TEXT NOT NULL DEFAULT 'pending',\n    \"createdAt\" TIMESTAMPTZ NOT NULL DEFAULT NOW()\n)",
    "CREATE TABLE \"AIReport\" (\n    \"id\" SERIAL NOT NULL PRIMARY KEY ,\n    \"userId\" INTEGER NOT NULL,\n    \"date\" TEXT NOT NULL DEFAULT '',\n    \"content\" TEXT NOT NULL,\n    \"createdAt\" TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    CONSTRAINT \"AIReport_userId_fkey\" FOREIGN KEY (\"userId\") REFERENCES \"User\" (\"id\") ON DELETE CASCADE ON UPDATE CASCADE\n)",
    "CREATE TABLE \"CodeSession\" (\n    \"id\" SERIAL NOT NULL PRIMARY KEY ,\n    \"userId\" INTEGER NOT NULL,\n    \"type\" TEXT NOT NULL DEFAULT 'explain',\n    \"track\" TEXT NOT NULL DEFAULT 'javascript',\n    \"input\" TEXT NOT NULL,\n    \"output\" TEXT NOT NULL,\n    \"createdAt\" TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    CONSTRAINT \"CodeSession_userId_fkey\" FOREIGN KEY (\"userId\") REFERENCES \"User\" (\"id\") ON DELETE CASCADE ON UPDATE CASCADE\n)",
    "CREATE TABLE \"DayLog\" (\n    \"id\" SERIAL NOT NULL PRIMARY KEY ,\n    \"date\" TEXT NOT NULL,\n    \"hours\" DOUBLE PRECISION NOT NULL DEFAULT 0,\n    \"topic\" TEXT NOT NULL DEFAULT '',\n    \"notes\" TEXT NOT NULL DEFAULT '',\n    \"createdAt\" TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    \"updatedAt\" TIMESTAMPTZ NOT NULL,\n    \"userId\" INTEGER NOT NULL,\n    CONSTRAINT \"DayLog_userId_fkey\" FOREIGN KEY (\"userId\") REFERENCES \"User\" (\"id\") ON DELETE CASCADE ON UPDATE CASCADE\n)",
    "CREATE TABLE \"EnglishLesson\" (\n    \"id\" SERIAL NOT NULL PRIMARY KEY ,\n    \"type\" TEXT NOT NULL,\n    \"content\" TEXT NOT NULL,\n    \"metadata\" TEXT NOT NULL DEFAULT '{}',\n    \"createdAt\" TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    \"userId\" INTEGER NOT NULL,\n    \"title\" TEXT NOT NULL DEFAULT '',\n    \"order\" INTEGER NOT NULL DEFAULT 0,\n    \"completed\" BOOLEAN NOT NULL DEFAULT false,\n    \"learnCount\" INTEGER NOT NULL DEFAULT 0,\n    \"nextReviewAt\" TIMESTAMPTZ,\n    \"lastReviewedAt\" TIMESTAMPTZ,\n    \"intervalDays\" INTEGER NOT NULL DEFAULT 0,\n    \"easeFactor\" DOUBLE PRECISION NOT NULL DEFAULT 2.5,\n    \"reviewCount\" INTEGER NOT NULL DEFAULT 0, strength INTEGER NOT NULL DEFAULT 0, gapNotes TEXT NOT NULL DEFAULT '',\n    CONSTRAINT \"EnglishLesson_userId_fkey\" FOREIGN KEY (\"userId\") REFERENCES \"User\" (\"id\") ON DELETE CASCADE ON UPDATE CASCADE\n)",
    "CREATE TABLE \"Lesson\" (\n    \"id\" SERIAL NOT NULL PRIMARY KEY ,\n    \"topic\" TEXT NOT NULL,\n    \"content\" TEXT NOT NULL,\n    \"createdAt\" TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    \"userId\" INTEGER NOT NULL,\n    \"track\" TEXT NOT NULL DEFAULT 'javascript',\n    \"order\" INTEGER NOT NULL DEFAULT 0,\n    \"completed\" BOOLEAN NOT NULL DEFAULT false,\n    \"learnCount\" INTEGER NOT NULL DEFAULT 0,\n    \"nextReviewAt\" TIMESTAMPTZ,\n    \"lastReviewedAt\" TIMESTAMPTZ,\n    \"intervalDays\" INTEGER NOT NULL DEFAULT 0,\n    \"easeFactor\" DOUBLE PRECISION NOT NULL DEFAULT 2.5,\n    \"reviewCount\" INTEGER NOT NULL DEFAULT 0, gapNotes TEXT NOT NULL DEFAULT '', strength INTEGER NOT NULL DEFAULT 0,\n    CONSTRAINT \"Lesson_userId_fkey\" FOREIGN KEY (\"userId\") REFERENCES \"User\" (\"id\") ON DELETE CASCADE ON UPDATE CASCADE\n)",
    "CREATE TABLE \"MindmapNote\" (\n    \"id\" SERIAL NOT NULL PRIMARY KEY ,\n    \"userId\" INTEGER NOT NULL,\n    \"date\" TEXT NOT NULL,\n    \"topic\" TEXT NOT NULL DEFAULT '',\n    \"title\" TEXT NOT NULL DEFAULT '',\n    \"markdown\" TEXT NOT NULL DEFAULT '',\n    \"createdAt\" TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    \"updatedAt\" TIMESTAMPTZ NOT NULL,\n    CONSTRAINT \"MindmapNote_userId_fkey\" FOREIGN KEY (\"userId\") REFERENCES \"User\" (\"id\") ON DELETE CASCADE ON UPDATE CASCADE\n)",
    "CREATE TABLE \"MissionConfig\" (\n    \"id\" SERIAL NOT NULL PRIMARY KEY ,\n    \"startDate\" TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    \"totalHours\" DOUBLE PRECISION NOT NULL DEFAULT 0,\n    \"userId\" INTEGER NOT NULL,\n    CONSTRAINT \"MissionConfig_userId_fkey\" FOREIGN KEY (\"userId\") REFERENCES \"User\" (\"id\") ON DELETE CASCADE ON UPDATE CASCADE\n)",
    "CREATE TABLE \"PomodoroSession\" (\n    \"id\" SERIAL NOT NULL PRIMARY KEY ,\n    \"date\" TEXT NOT NULL,\n    \"sessions\" INTEGER NOT NULL DEFAULT 0,\n    \"updatedAt\" TIMESTAMPTZ NOT NULL,\n    \"userId\" INTEGER NOT NULL,\n    \"currentEndTime\" BIGINT DEFAULT 0,\n    \"currentMode\" TEXT NOT NULL DEFAULT 'work',\n    CONSTRAINT \"PomodoroSession_userId_fkey\" FOREIGN KEY (\"userId\") REFERENCES \"User\" (\"id\") ON DELETE CASCADE ON UPDATE CASCADE\n)",
    "CREATE TABLE \"RoadmapItem\" (\n    \"id\" TEXT NOT NULL PRIMARY KEY,\n    \"completed\" BOOLEAN NOT NULL DEFAULT false,\n    \"updatedAt\" TIMESTAMPTZ NOT NULL,\n    \"userId\" INTEGER NOT NULL,\n    CONSTRAINT \"RoadmapItem_userId_fkey\" FOREIGN KEY (\"userId\") REFERENCES \"User\" (\"id\") ON DELETE CASCADE ON UPDATE CASCADE\n)",
    "CREATE TABLE \"Settings\" (\n    \"id\" SERIAL NOT NULL PRIMARY KEY  ,\n    \"aiServer\" TEXT NOT NULL DEFAULT 'http://100.69.50.64:8080/v1',\n    \"aiHost\" TEXT NOT NULL DEFAULT '100.69.50.64',\n    \"aiProvider\" TEXT NOT NULL DEFAULT 'local',\n    \"aiModel\" TEXT NOT NULL DEFAULT 'default',\n    \"aiKey\" TEXT NOT NULL DEFAULT '',\n    \"updatedAt\" TIMESTAMPTZ NOT NULL DEFAULT NOW()\n)",
    "CREATE TABLE \"agent_goals\" (\n                id TEXT PRIMARY KEY,\n                user_id TEXT NOT NULL,\n                title TEXT NOT NULL,\n                description TEXT NOT NULL DEFAULT '',\n                status TEXT NOT NULL DEFAULT 'active',\n                priority INTEGER NOT NULL DEFAULT 3,\n                deadline TEXT,\n                progress INTEGER NOT NULL DEFAULT 0,\n                source TEXT NOT NULL DEFAULT 'manual',\n                metadata_json TEXT,\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                updated_at TEXT NOT NULL DEFAULT NOW(),\n                completed_at TEXT,\n                archived_at TEXT\n            )",
    "CREATE TABLE hagent_users (\n    id TEXT PRIMARY KEY,\n    username TEXT UNIQUE NOT NULL,\n    password_hash TEXT NOT NULL,\n    display_name TEXT DEFAULT '',\n    created_at TEXT DEFAULT NOW(),\n    updated_at TEXT DEFAULT NOW()\n  , default_provider TEXT DEFAULT 'deepseek', claude_mode TEXT DEFAULT 'qwen', pinned_folders TEXT DEFAULT '[]', email TEXT DEFAULT '', avatar TEXT DEFAULT '', default_agent TEXT DEFAULT '', account_status TEXT DEFAULT 'active', expires_at TEXT DEFAULT '', role TEXT DEFAULT 'user')",
    "CREATE TABLE \"agents\" (\n    id TEXT PRIMARY KEY,\n    user_id TEXT NOT NULL,\n    name TEXT NOT NULL,\n    description TEXT DEFAULT '',\n    model TEXT DEFAULT 'local',\n    soul_content TEXT DEFAULT '',\n    is_public BOOLEAN DEFAULT FALSE,\n    created_at TEXT DEFAULT NOW(),\n    updated_at TEXT DEFAULT NOW(), tool_groups TEXT DEFAULT '[]', skills TEXT DEFAULT '[]', is_active INTEGER DEFAULT 0, auto_start INTEGER DEFAULT 0, last_run_at TEXT, interval_seconds INTEGER DEFAULT 300,\n    FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"agent_todos\" (\n    id TEXT PRIMARY KEY,\n    agent_id TEXT NOT NULL,\n    content TEXT NOT NULL,\n    status TEXT DEFAULT 'pending' CHECK(status IN ('pending', 'in_progress', 'completed', 'failed')),\n    result TEXT DEFAULT '',\n    created_at TEXT DEFAULT NOW(),\n    updated_at TEXT DEFAULT NOW(),\n    FOREIGN KEY (agent_id) REFERENCES agents(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"audit_log\" (\n                id SERIAL PRIMARY KEY ,\n                created_at TEXT DEFAULT NOW(),\n                actor_id TEXT DEFAULT '',\n                actor_name TEXT DEFAULT '',\n                action TEXT NOT NULL,\n                target_type TEXT DEFAULT '',\n                target_id TEXT DEFAULT '',\n                detail TEXT DEFAULT '',\n                ip_address TEXT DEFAULT ''\n            )",
    "CREATE TABLE \"auto_fetch_sources\" (\n                id TEXT PRIMARY KEY,\n                user_id TEXT NOT NULL,\n                name TEXT NOT NULL,\n                kind TEXT NOT NULL DEFAULT 'rss',\n                url TEXT NOT NULL,\n                config_json TEXT NOT NULL DEFAULT '{}',\n                enabled INTEGER NOT NULL DEFAULT 1,\n                interval_seconds INTEGER NOT NULL DEFAULT 1200,\n                last_run_at TEXT,\n                next_run_at TEXT,\n                last_status_json TEXT,\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                updated_at TEXT NOT NULL DEFAULT NOW()\n            )",
    "CREATE TABLE \"auto_fetch_seen\" (\n                source_id TEXT NOT NULL,\n                fingerprint TEXT NOT NULL,\n                first_seen_at TEXT NOT NULL DEFAULT NOW(),\n                PRIMARY KEY (source_id, fingerprint),\n                FOREIGN KEY (source_id) REFERENCES auto_fetch_sources(id) ON DELETE CASCADE\n            )",
    "CREATE TABLE \"editor_projects\" (\n                id SERIAL PRIMARY KEY ,\n                user_id TEXT NOT NULL,\n                title TEXT NOT NULL,\n                width INTEGER DEFAULT 1920,\n                height INTEGER DEFAULT 1080,\n                fps INTEGER DEFAULT 30,\n                duration DOUBLE PRECISION DEFAULT 0,\n                timeline_json TEXT DEFAULT '{}',\n                created_at BIGINT NOT NULL,\n                updated_at BIGINT NOT NULL\n            , watermark_json TEXT)",
    "CREATE TABLE \"auto_video_jobs\" (\n                id SERIAL PRIMARY KEY ,\n                project_id INTEGER REFERENCES editor_projects(id) ON DELETE CASCADE,\n                render_job_id INTEGER,\n                user_id TEXT NOT NULL,\n                topic TEXT NOT NULL,\n                status TEXT NOT NULL DEFAULT 'running',\n                step TEXT,\n                current_scene INTEGER DEFAULT 0,\n                total_scenes INTEGER DEFAULT 0,\n                message TEXT,\n                output_path TEXT,\n                created_at BIGINT NOT NULL,\n                updated_at BIGINT NOT NULL\n            )",
    "CREATE TABLE \"cached_jobs\" (\n                url TEXT PRIMARY KEY,\n                title TEXT NOT NULL,\n                company TEXT NOT NULL,\n                location TEXT,\n                salary TEXT,\n                salary_min INTEGER,\n                salary_max INTEGER,\n                source TEXT NOT NULL DEFAULT '',\n                posted_date TEXT,\n                skills TEXT DEFAULT '[]',\n                description_snippet TEXT,\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                updated_at TEXT NOT NULL DEFAULT NOW()\n            )",
    "CREATE TABLE \"chat_sessions\" (\n    id TEXT PRIMARY KEY,\n    user_id TEXT NOT NULL,\n    title TEXT DEFAULT 'New Chat',\n    created_at TEXT DEFAULT NOW(),\n    updated_at TEXT DEFAULT NOW(), agent_id TEXT REFERENCES agents(id) ON DELETE SET NULL, processing INTEGER DEFAULT 0, summary TEXT, parent_session_id TEXT,\n    FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"chat_sessions_new\" (\n    id TEXT PRIMARY KEY,\n    user_id TEXT NOT NULL,\n    agent_id TEXT,\n    title TEXT DEFAULT 'New Chat',\n    created_at TEXT DEFAULT NOW(),\n    updated_at TEXT DEFAULT NOW(),\n    FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE,\n    FOREIGN KEY (agent_id) REFERENCES agents(id) ON DELETE SET NULL\n  )",
    "CREATE TABLE \"coach_reminders\" (\n                id SERIAL PRIMARY KEY ,\n                user_id TEXT NOT NULL,\n                kind TEXT NOT NULL DEFAULT 'hourly',\n                message TEXT NOT NULL DEFAULT '',\n                meta_json TEXT NOT NULL DEFAULT '{}',\n                delivered_channels TEXT NOT NULL DEFAULT '',\n                read_at TEXT,\n                created_at TEXT NOT NULL DEFAULT NOW()\n            )",
    "CREATE TABLE \"custom_providers\" (\n    id TEXT PRIMARY KEY,\n    user_id TEXT NOT NULL,\n    name TEXT NOT NULL,\n    label TEXT NOT NULL,\n    type TEXT DEFAULT 'openai',\n    base_url TEXT DEFAULT '',\n    api_key TEXT DEFAULT '',\n    model TEXT DEFAULT '',\n    created_at TEXT DEFAULT NOW(),\n    updated_at TEXT DEFAULT NOW(), context_length INTEGER DEFAULT NULL,\n    FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"cv_documents\" (\n                id TEXT PRIMARY KEY,\n                user_id TEXT NOT NULL,\n                filename TEXT NOT NULL,\n                file_path TEXT NOT NULL,\n                content_type TEXT NOT NULL DEFAULT '',\n                extracted_text TEXT NOT NULL DEFAULT '',\n                result_json TEXT NOT NULL DEFAULT '{}',\n                created_at TEXT NOT NULL DEFAULT NOW()\n            , pathway TEXT NOT NULL DEFAULT '')",
    "CREATE TABLE \"cv_profiles\" (\n    id TEXT PRIMARY KEY,\n    user_id TEXT NOT NULL,\n    name TEXT NOT NULL,\n    file_name TEXT NOT NULL,\n    file_path TEXT NOT NULL,\n    raw_text_old TEXT NOT NULL,\n    summary TEXT DEFAULT '',\n    skills_json TEXT DEFAULT '[]',\n    roles_json TEXT DEFAULT '[]',\n    locations_json TEXT DEFAULT '[]',\n    created_at TEXT DEFAULT NOW(),\n    updated_at TEXT DEFAULT NOW(), raw_text TEXT DEFAULT '', parsed_data TEXT DEFAULT '{}',\n    FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"cv_job_applications\" (\n    id TEXT PRIMARY KEY,\n    user_id TEXT NOT NULL,\n    profile_id TEXT NOT NULL,\n    search_id TEXT DEFAULT '',\n    job_url TEXT NOT NULL,\n    job_title TEXT NOT NULL,\n    company TEXT DEFAULT '',\n    source TEXT DEFAULT '',\n    status TEXT DEFAULT 'pending_review',\n    match_score BIGINT DEFAULT 0,\n    income_potential INTEGER DEFAULT 0,\n    verdict TEXT DEFAULT '',\n    job_json TEXT DEFAULT '{}',\n    draft_message TEXT DEFAULT '',\n    notes TEXT DEFAULT '',\n    created_at TEXT DEFAULT NOW(),\n    updated_at TEXT DEFAULT NOW(),\n    FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE,\n    FOREIGN KEY (profile_id) REFERENCES cv_profiles(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"cv_job_searches\" (\n    id TEXT PRIMARY KEY,\n    user_id TEXT NOT NULL,\n    profile_id TEXT NOT NULL,\n    query TEXT NOT NULL,\n    location TEXT DEFAULT '',\n    results_json TEXT DEFAULT '[]',\n    created_at TEXT DEFAULT NOW(),\n    FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE,\n    FOREIGN KEY (profile_id) REFERENCES cv_profiles(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"cv_match_scores\" (\n                user_id TEXT NOT NULL,\n                job_url TEXT NOT NULL,\n                match_score BIGINT NOT NULL DEFAULT 0,\n                verdict TEXT NOT NULL DEFAULT '',\n                matched_json TEXT NOT NULL DEFAULT '[]',\n                missing_json TEXT NOT NULL DEFAULT '[]',\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                updated_at TEXT NOT NULL DEFAULT NOW(),\n                PRIMARY KEY (user_id, job_url)\n            )",
    "CREATE TABLE \"deleted_jobs\" (\n                url TEXT PRIMARY KEY,\n                deleted_at TEXT NOT NULL DEFAULT NOW()\n            )",
    "CREATE TABLE \"devices\" (\n                id TEXT PRIMARY KEY,\n                user_id TEXT NOT NULL,\n                secret_hash TEXT DEFAULT '',\n                device_name TEXT DEFAULT '',\n                user_agent TEXT DEFAULT '',\n                first_ip_address TEXT DEFAULT '',\n                last_ip_address TEXT DEFAULT '',\n                status TEXT DEFAULT 'pending',\n                created_at TEXT DEFAULT NOW(),\n                last_active TEXT,\n                FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE\n            )",
    "CREATE TABLE \"drive_backup_maps\" (\n                id TEXT PRIMARY KEY,\n                user_id TEXT NOT NULL,\n                name TEXT NOT NULL DEFAULT '',\n                source_path TEXT NOT NULL,\n                account_ids_json TEXT NOT NULL DEFAULT '[]',\n                enabled INTEGER NOT NULL DEFAULT 1,\n                last_run_at TEXT DEFAULT '',\n                last_status TEXT DEFAULT '',\n                last_files INTEGER DEFAULT 0,\n                last_bytes BIGINT DEFAULT 0,\n                last_skipped INTEGER DEFAULT 0,\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                updated_at TEXT NOT NULL DEFAULT NOW()\n            , dest_folder TEXT NOT NULL DEFAULT '', delete_source_after_sync INTEGER NOT NULL DEFAULT 0, source_paths_json TEXT NOT NULL DEFAULT '[]', dest_folders_json TEXT NOT NULL DEFAULT '{}', schedule_interval TEXT NOT NULL DEFAULT 'daily_2')",
    "CREATE TABLE \"drive_backup_runs\" (\n                id TEXT PRIMARY KEY,\n                map_id TEXT NOT NULL,\n                trigger TEXT NOT NULL DEFAULT 'manual',\n                status TEXT NOT NULL DEFAULT '',\n                files_done INTEGER DEFAULT 0,\n                bytes_done BIGINT DEFAULT 0,\n                skipped INTEGER DEFAULT 0,\n                errors_json TEXT DEFAULT '[]',\n                started_at TEXT DEFAULT '',\n                finished_at TEXT DEFAULT ''\n            )",
    "CREATE TABLE \"editor_assets\" (\n                id SERIAL PRIMARY KEY ,\n                project_id INTEGER NOT NULL REFERENCES editor_projects(id) ON DELETE CASCADE,\n                kind TEXT NOT NULL,\n                path TEXT NOT NULL,\n                name TEXT NOT NULL,\n                duration DOUBLE PRECISION DEFAULT 0,\n                width INTEGER DEFAULT 0,\n                height INTEGER DEFAULT 0,\n                created_at BIGINT NOT NULL\n            )",
    "CREATE TABLE \"editor_render_jobs\" (\n                id SERIAL PRIMARY KEY ,\n                project_id INTEGER NOT NULL REFERENCES editor_projects(id) ON DELETE CASCADE,\n                status TEXT NOT NULL DEFAULT 'queued',\n                progress INTEGER DEFAULT 0,\n                output_path TEXT,\n                error TEXT,\n                created_at BIGINT NOT NULL,\n                updated_at BIGINT NOT NULL\n            )",
    "CREATE TABLE \"english_items\" (\n                id TEXT PRIMARY KEY,\n                user_id TEXT NOT NULL,\n                type TEXT NOT NULL DEFAULT 'vocab',\n                content TEXT NOT NULL DEFAULT '',\n                title TEXT NOT NULL DEFAULT '',\n                metadata TEXT NOT NULL DEFAULT '{}',\n                completed INTEGER NOT NULL DEFAULT 0,\n                learn_count INTEGER NOT NULL DEFAULT 0,\n                quiz_score INTEGER,\n                quiz_total INTEGER,\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                updated_at TEXT NOT NULL DEFAULT NOW()\n            )",
    "CREATE TABLE \"entertainment_video_captions\" (\n                id SERIAL PRIMARY KEY ,\n                user_id TEXT NOT NULL,\n                url_key TEXT NOT NULL,\n                title TEXT NOT NULL DEFAULT '',\n                source_lang TEXT NOT NULL DEFAULT '',\n                segments_json TEXT NOT NULL DEFAULT '[]',\n                created_at BIGINT NOT NULL,\n                updated_at BIGINT NOT NULL,\n                UNIQUE(user_id, url_key)\n            )",
    "CREATE TABLE \"entertainment_video_categories\" (\n                id TEXT NOT NULL,\n                user_id TEXT NOT NULL,\n                label TEXT NOT NULL,\n                sort_order INTEGER NOT NULL DEFAULT 0,\n                created_at BIGINT NOT NULL,\n                updated_at BIGINT NOT NULL,\n                PRIMARY KEY (user_id, id)\n            )",
    "CREATE TABLE \"entertainment_videos\" (\n                id SERIAL PRIMARY KEY ,\n                user_id TEXT NOT NULL,\n                title TEXT NOT NULL DEFAULT '',\n                input TEXT NOT NULL DEFAULT '',\n                src TEXT NOT NULL DEFAULT '',\n                open_url TEXT NOT NULL DEFAULT '',\n                video_type TEXT NOT NULL DEFAULT 'embed',\n                created_at BIGINT NOT NULL,\n                updated_at BIGINT NOT NULL, progress_position DOUBLE PRECISION NOT NULL DEFAULT 0, progress_duration DOUBLE PRECISION NOT NULL DEFAULT 0, watched_at BIGINT NOT NULL DEFAULT 0, category TEXT NOT NULL DEFAULT 'other', source_lang TEXT NOT NULL DEFAULT '',\n                UNIQUE(user_id, src)\n            )",
    "CREATE TABLE \"goal_tasks\" (\n                id TEXT PRIMARY KEY,\n                goal_id TEXT NOT NULL,\n                user_id TEXT NOT NULL,\n                title TEXT NOT NULL,\n                detail TEXT NOT NULL DEFAULT '',\n                status TEXT NOT NULL DEFAULT 'pending',\n                priority INTEGER NOT NULL DEFAULT 3,\n                evidence TEXT NOT NULL DEFAULT '',\n                result TEXT NOT NULL DEFAULT '',\n                last_attempt_at TEXT,\n                completed_at TEXT,\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                updated_at TEXT NOT NULL DEFAULT NOW(),\n                FOREIGN KEY (goal_id) REFERENCES agent_goals(id) ON DELETE CASCADE\n            )",
    "CREATE TABLE \"google_account_inventory\" (\n                id TEXT PRIMARY KEY,\n                user_id TEXT NOT NULL,\n                email TEXT NOT NULL,\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                updated_at TEXT NOT NULL DEFAULT NOW(),\n                UNIQUE(user_id, email)\n            )",
    "CREATE TABLE \"google_accounts\" (\n                id TEXT PRIMARY KEY,\n                user_id TEXT NOT NULL,\n                email TEXT NOT NULL,\n                token_path TEXT NOT NULL,\n                scopes_json TEXT NOT NULL DEFAULT '[]',\n                enabled_for_agent INTEGER NOT NULL DEFAULT 1,\n                is_default INTEGER NOT NULL DEFAULT 0,\n                last_status TEXT NOT NULL DEFAULT 'connected',\n                last_error TEXT DEFAULT '',\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                updated_at TEXT NOT NULL DEFAULT NOW(), credential_encrypted TEXT NOT NULL DEFAULT '',\n                UNIQUE(user_id, email)\n            )",
    "CREATE TABLE \"google_email_keepalive_config\" (\n                user_id TEXT PRIMARY KEY,\n                target_email TEXT NOT NULL,\n                enabled INTEGER NOT NULL DEFAULT 1,\n                schedule_day INTEGER NOT NULL DEFAULT 15,\n                schedule_hour INTEGER NOT NULL DEFAULT 9,\n                last_run_month TEXT NOT NULL DEFAULT '',\n                last_run_at TEXT NOT NULL DEFAULT '',\n                last_status TEXT NOT NULL DEFAULT '',\n                last_error TEXT NOT NULL DEFAULT '',\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                updated_at TEXT NOT NULL DEFAULT NOW()\n            )",
    "CREATE TABLE \"google_email_keepalive_deliveries\" (\n                    id TEXT PRIMARY KEY,\n                    user_id TEXT NOT NULL,\n                    month_key TEXT NOT NULL,\n                    direction_key TEXT NOT NULL,\n                    sender_email TEXT NOT NULL,\n                    recipient_email TEXT NOT NULL,\n                    status TEXT NOT NULL DEFAULT 'pending',\n                    message_id TEXT NOT NULL DEFAULT '',\n                    error TEXT NOT NULL DEFAULT '',\n                    created_at TEXT NOT NULL DEFAULT NOW(),\n                    updated_at TEXT NOT NULL DEFAULT NOW(),\n                    UNIQUE(user_id, month_key, direction_key)\n                )",
    "CREATE TABLE \"google_oauth_pending\" (\n                state TEXT PRIMARY KEY,\n                user_id TEXT NOT NULL,\n                code_verifier TEXT DEFAULT '',\n                created_at DOUBLE PRECISION NOT NULL\n            , redirect_uri TEXT DEFAULT '', scopes_json TEXT NOT NULL DEFAULT '[]')",
    "CREATE TABLE \"google_oauth_results\" (\n                state TEXT PRIMARY KEY,\n                user_id TEXT NOT NULL,\n                status TEXT NOT NULL,\n                account_json TEXT DEFAULT '',\n                error TEXT DEFAULT '',\n                updated_at TEXT NOT NULL DEFAULT NOW()\n            )",
    "CREATE TABLE \"learn_ai_reports\" (\n                id SERIAL PRIMARY KEY ,\n                user_id TEXT NOT NULL,\n                date TEXT NOT NULL DEFAULT '',\n                content TEXT NOT NULL DEFAULT '',\n                created_at TEXT NOT NULL DEFAULT NOW()\n            )",
    "CREATE TABLE \"learn_code_sessions\" (\n                id SERIAL PRIMARY KEY ,\n                user_id TEXT NOT NULL,\n                type TEXT NOT NULL DEFAULT 'explain',\n                track TEXT NOT NULL DEFAULT 'javascript',\n                input TEXT NOT NULL DEFAULT '',\n                output TEXT NOT NULL DEFAULT '',\n                created_at TEXT NOT NULL DEFAULT NOW()\n            )",
    "CREATE TABLE \"learn_day_logs\" (\n                id SERIAL PRIMARY KEY ,\n                user_id TEXT NOT NULL,\n                date TEXT NOT NULL,\n                hours DOUBLE PRECISION NOT NULL DEFAULT 0,\n                topic TEXT NOT NULL DEFAULT '',\n                notes TEXT NOT NULL DEFAULT '',\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                updated_at TEXT NOT NULL DEFAULT NOW(),\n                UNIQUE(user_id, date)\n            )",
    "CREATE TABLE \"learn_lessons\" (\n                id TEXT PRIMARY KEY,\n                user_id TEXT NOT NULL,\n                track TEXT NOT NULL DEFAULT 'javascript',\n                topic TEXT NOT NULL,\n                content TEXT NOT NULL DEFAULT '',\n                \"order\" INTEGER NOT NULL DEFAULT 0,\n                completed INTEGER NOT NULL DEFAULT 0,\n                learn_count INTEGER NOT NULL DEFAULT 0,\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                updated_at TEXT NOT NULL DEFAULT NOW()\n            )",
    "CREATE TABLE \"learn_mission\" (\n                id SERIAL PRIMARY KEY ,\n                user_id TEXT UNIQUE NOT NULL,\n                start_date TEXT NOT NULL DEFAULT NOW(),\n                total_hours DOUBLE PRECISION NOT NULL DEFAULT 0,\n                created_at TEXT NOT NULL DEFAULT NOW()\n            )",
    "CREATE TABLE \"learn_pomodoro\" (\n                id SERIAL PRIMARY KEY ,\n                user_id TEXT NOT NULL,\n                date TEXT NOT NULL,\n                sessions INTEGER NOT NULL DEFAULT 0,\n                current_end_time BIGINT NOT NULL DEFAULT 0,\n                current_mode TEXT NOT NULL DEFAULT 'work',\n                updated_at TEXT NOT NULL DEFAULT NOW(),\n                UNIQUE(user_id, date)\n            )",
    "CREATE TABLE \"learn_roadmap\" (\n                id TEXT NOT NULL,\n                user_id TEXT NOT NULL,\n                completed INTEGER NOT NULL DEFAULT 0,\n                updated_at TEXT NOT NULL DEFAULT NOW(),\n                PRIMARY KEY (user_id, id)\n            )",
    "CREATE TABLE \"learn_settings\" (\n                id INTEGER PRIMARY KEY,\n                ai_server TEXT NOT NULL DEFAULT 'http://100.69.50.64:8080/v1',\n                ai_host TEXT NOT NULL DEFAULT '100.69.50.64',\n                ai_provider TEXT NOT NULL DEFAULT 'local',\n                ai_model TEXT NOT NULL DEFAULT 'default',\n                ai_key TEXT NOT NULL DEFAULT '',\n                updated_at TEXT NOT NULL DEFAULT NOW()\n            )",
    "CREATE TABLE \"message_feedback\" (\n                id TEXT PRIMARY KEY,\n                user_id TEXT NOT NULL,\n                session_id TEXT NOT NULL,\n                message_id TEXT NOT NULL,\n                rating TEXT NOT NULL,\n                comment TEXT NOT NULL DEFAULT '',\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                UNIQUE(user_id, message_id)\n            )",
    "CREATE TABLE \"messages\" (\n    id TEXT PRIMARY KEY,\n    session_id TEXT NOT NULL,\n    user_id TEXT NOT NULL,\n    role TEXT NOT NULL CHECK(role IN ('user', 'assistant', 'system')),\n    content TEXT NOT NULL,\n    provider TEXT DEFAULT 'anthropic',\n    created_at TEXT DEFAULT NOW(), usage_json TEXT, tool_results TEXT DEFAULT '',\n    FOREIGN KEY (session_id) REFERENCES chat_sessions(id) ON DELETE CASCADE,\n    FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"music_tracks\" (\n                id TEXT PRIMARY KEY,\n                filename TEXT NOT NULL,\n                title TEXT NOT NULL,\n                duration DOUBLE PRECISION DEFAULT 0,\n                size BIGINT DEFAULT 0,\n                created_at TEXT DEFAULT NOW()\n            )",
    "CREATE TABLE \"omni_conversations\" (\n                id TEXT PRIMARY KEY,\n                user_id TEXT NOT NULL,\n                platform TEXT NOT NULL,\n                external_id TEXT,\n                title TEXT NOT NULL DEFAULT 'Unknown',\n                custom_name TEXT,\n                pinned INTEGER NOT NULL DEFAULT 0,\n                unread_count INTEGER NOT NULL DEFAULT 0,\n                last_message_preview TEXT,\n                last_message_sender TEXT,\n                last_message_at TEXT,\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                updated_at TEXT NOT NULL DEFAULT NOW()\n            , thread_type TEXT DEFAULT 'user', avatar_url TEXT DEFAULT '')",
    "CREATE TABLE \"omni_agent_auto_reply\" (\n                user_id TEXT NOT NULL,\n                conversation_id TEXT NOT NULL,\n                enabled INTEGER NOT NULL DEFAULT 0,\n                session_id TEXT NOT NULL,\n                provider TEXT,\n                model TEXT,\n                last_processed_message_id TEXT,\n                last_error TEXT DEFAULT '',\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                updated_at TEXT NOT NULL DEFAULT NOW(),\n                PRIMARY KEY (user_id, conversation_id),\n                FOREIGN KEY (conversation_id) REFERENCES omni_conversations(id) ON DELETE CASCADE\n            )",
    "CREATE TABLE \"omni_channels\" (\n    id TEXT PRIMARY KEY,\n    user_id TEXT NOT NULL,\n    name TEXT NOT NULL,\n    platform TEXT NOT NULL,\n    access_token TEXT DEFAULT '',\n    refresh_token TEXT DEFAULT '',\n    is_active INTEGER DEFAULT 1,\n    pin TEXT DEFAULT '',\n    is_licensed INTEGER DEFAULT 0,\n    license_key TEXT DEFAULT '',\n    created_at TEXT DEFAULT NOW(),\n    updated_at TEXT DEFAULT NOW(), auto_reply INTEGER DEFAULT 0,\n    FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"omni_contacts\" (\n                id TEXT PRIMARY KEY,\n                user_id TEXT NOT NULL,\n                platform TEXT NOT NULL,\n                external_id TEXT,\n                name TEXT NOT NULL,\n                avatar_url TEXT,\n                last_message_at TEXT,\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                UNIQUE(user_id, platform, external_id)\n            )",
    "CREATE TABLE \"omni_hidden_contacts\" (\n                user_id TEXT NOT NULL,\n                platform TEXT NOT NULL,\n                external_id TEXT NOT NULL,\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                PRIMARY KEY (user_id, platform, external_id)\n            )",
    "CREATE TABLE \"omni_messages\" (\n                id TEXT PRIMARY KEY,\n                conversation_id TEXT NOT NULL,\n                user_id TEXT NOT NULL,\n                role TEXT NOT NULL DEFAULT 'user',\n                content TEXT NOT NULL,\n                reply_to_id TEXT,\n                reactions_json TEXT,\n                platform TEXT,\n                external_id TEXT,\n                created_at TEXT NOT NULL DEFAULT NOW(), external_cli_msg_id TEXT, external_msg_type TEXT, external_author_id TEXT, external_author_name TEXT,\n                FOREIGN KEY (conversation_id) REFERENCES omni_conversations(id) ON DELETE CASCADE\n            )",
    "CREATE TABLE \"omni_sync_codes\" (\n    id TEXT PRIMARY KEY,\n    code TEXT NOT NULL UNIQUE,\n    user_id TEXT NOT NULL,\n    used INTEGER DEFAULT 0,\n    created_at TEXT DEFAULT NOW(),\n    FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"reup_jobs\" (\n                id SERIAL PRIMARY KEY ,\n                user_id TEXT NOT NULL,\n                source_path TEXT NOT NULL,\n                output_path TEXT,\n                status TEXT NOT NULL DEFAULT 'queued',\n                progress INTEGER DEFAULT 0,\n                error TEXT,\n                created_at BIGINT NOT NULL,\n                updated_at BIGINT NOT NULL\n            )",
    "CREATE TABLE \"role_permissions\" (\n                role TEXT PRIMARY KEY,\n                label TEXT DEFAULT '',\n                tabs TEXT DEFAULT '[]',\n                is_system INTEGER DEFAULT 0,\n                created_at TEXT DEFAULT NOW(),\n                updated_at TEXT DEFAULT NOW()\n            )",
    "CREATE TABLE \"run_journals\" (\n    id SERIAL PRIMARY KEY ,\n    message_id TEXT NOT NULL,\n    session_id TEXT NOT NULL,\n    type TEXT NOT NULL,\n    event_name TEXT,\n    content TEXT,\n    status TEXT,\n    count INTEGER,\n    created_at TEXT DEFAULT NOW(), input TEXT, output TEXT,\n    FOREIGN KEY (message_id) REFERENCES messages(id) ON DELETE CASCADE,\n    FOREIGN KEY (session_id) REFERENCES chat_sessions(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"scrape_runs\" (\n                    id SERIAL PRIMARY KEY ,\n                    source TEXT NOT NULL,\n                    keyword TEXT NOT NULL DEFAULT '',\n                    count INTEGER NOT NULL DEFAULT 0,\n                    ok INTEGER NOT NULL DEFAULT 1,\n                    error TEXT,\n                    created_at TEXT NOT NULL DEFAULT NOW()\n                )",
    "CREATE TABLE \"self_evolution\" (\n    id SERIAL PRIMARY KEY ,\n    key TEXT NOT NULL UNIQUE,\n    content TEXT NOT NULL,\n    updated_at TEXT DEFAULT NOW()\n  , metadata_json TEXT)",
    "CREATE TABLE \"self_evolution_events\" (\n                id TEXT PRIMARY KEY,\n                user_id TEXT NOT NULL,\n                event_type TEXT NOT NULL,\n                source TEXT NOT NULL DEFAULT 'reflection',\n                source_session_id TEXT,\n                source_message_id TEXT,\n                related_message_id TEXT,\n                title TEXT NOT NULL,\n                evidence TEXT NOT NULL DEFAULT '',\n                lesson TEXT NOT NULL DEFAULT '',\n                action TEXT NOT NULL DEFAULT '',\n                confidence DOUBLE PRECISION NOT NULL DEFAULT 0.5,\n                status TEXT NOT NULL DEFAULT 'pending',\n                metadata_json TEXT,\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                updated_at TEXT NOT NULL DEFAULT NOW(),\n                applied_at TEXT\n            )",
    "CREATE TABLE \"service_port_labels\" (\n                user_id TEXT NOT NULL,\n                port INTEGER NOT NULL,\n                project TEXT NOT NULL DEFAULT '',\n                label TEXT NOT NULL DEFAULT '',\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                updated_at TEXT NOT NULL DEFAULT NOW(),\n                PRIMARY KEY (user_id, port)\n            )",
    "CREATE TABLE \"session_todos\" (\n    id TEXT PRIMARY KEY,\n    session_id TEXT NOT NULL,\n    content TEXT NOT NULL,\n    status TEXT NOT NULL DEFAULT 'pending' CHECK(status IN ('pending', 'in_progress', 'completed', 'cancelled')),\n    created_at TEXT DEFAULT NOW(),\n    updated_at TEXT DEFAULT NOW(),\n    FOREIGN KEY (session_id) REFERENCES chat_sessions(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"sessions\" (\n    id TEXT PRIMARY KEY,\n    user_id TEXT NOT NULL,\n    created_at TEXT DEFAULT NOW(),\n    expires_at TEXT, ip_address TEXT DEFAULT '', user_agent TEXT DEFAULT '', device_name TEXT DEFAULT '', last_active TEXT, status TEXT DEFAULT 'approved', device_id TEXT DEFAULT '',\n    FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"state_meta\" (key TEXT PRIMARY KEY, value TEXT)",
    "CREATE TABLE \"stories\" (\n                id SERIAL PRIMARY KEY ,\n                slug TEXT UNIQUE NOT NULL,\n                title TEXT NOT NULL,\n                author TEXT DEFAULT '',\n                translator_group TEXT DEFAULT '',\n                status TEXT DEFAULT '',\n                genres TEXT DEFAULT '',\n                description TEXT DEFAULT '',\n                cover_url TEXT DEFAULT '',\n                chapter_count INTEGER DEFAULT 0,\n                source TEXT DEFAULT 'truyencv',\n                created_at BIGINT NOT NULL,\n                updated_at BIGINT NOT NULL\n            , last_chapter TEXT DEFAULT '', last_chapter_time TEXT DEFAULT '', version_id INTEGER)",
    "CREATE TABLE \"story_chapters\" (\n                id SERIAL PRIMARY KEY ,\n                story_slug TEXT NOT NULL REFERENCES stories(slug) ON DELETE CASCADE,\n                slug TEXT NOT NULL,\n                title TEXT NOT NULL,\n                chapter_number INTEGER DEFAULT 0,\n                content TEXT DEFAULT '',\n                updated_time TEXT DEFAULT '',\n                created_at BIGINT NOT NULL,\n                UNIQUE(story_slug, slug)\n            )",
    "CREATE TABLE \"tasks\" (\n    id TEXT PRIMARY KEY,\n    user_id TEXT NOT NULL,\n    name TEXT NOT NULL,\n    description TEXT DEFAULT '',\n    prompt TEXT NOT NULL,\n    schedule TEXT DEFAULT 'manual',\n    schedule_type TEXT DEFAULT 'interval' CHECK(schedule_type IN ('cron', 'interval', 'once', 'manual')),\n    interval_seconds INTEGER DEFAULT 3600,\n    skills TEXT DEFAULT '[]',\n    tools TEXT DEFAULT '[]',\n    max_iterations BIGINT DEFAULT 5,\n    max_retries INTEGER DEFAULT 3,\n    state TEXT DEFAULT 'scheduled' CHECK(state IN ('scheduled', 'running', 'paused', 'completed', 'failed')),\n    context TEXT DEFAULT '{}',\n    results TEXT DEFAULT '[]',\n    current_iteration BIGINT DEFAULT 0,\n    last_run_at TEXT,\n    last_error TEXT,\n    enabled INTEGER DEFAULT 0,\n    auto_start INTEGER DEFAULT 0,\n    delivery_platform TEXT,\n    delivery_target TEXT,\n    created_at TEXT DEFAULT NOW(),\n    updated_at TEXT DEFAULT NOW(),\n    FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"task_results\" (\n    id TEXT PRIMARY KEY,\n    task_id TEXT NOT NULL,\n    iteration BIGINT NOT NULL,\n    result TEXT,\n    status TEXT DEFAULT 'pending' CHECK(status IN ('pending', 'completed', 'failed', 'retrying')),\n    retry_count INTEGER DEFAULT 0,\n    created_at TEXT DEFAULT NOW(),\n    FOREIGN KEY (task_id) REFERENCES tasks(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"telegram_chat_links\" (\n    id TEXT PRIMARY KEY,\n    user_id TEXT NOT NULL,\n    chat_id TEXT NOT NULL,\n    bot_username TEXT DEFAULT '',\n    sender_name TEXT DEFAULT '',\n    created_at TEXT DEFAULT NOW(),\n    updated_at TEXT DEFAULT NOW(),\n    FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"telegram_config\" (\n    id SERIAL PRIMARY KEY ,\n    user_id TEXT NOT NULL UNIQUE,\n    bot_token TEXT NOT NULL,\n    bot_username TEXT DEFAULT '',\n    webhook_url TEXT DEFAULT '',\n    active INTEGER DEFAULT 1,\n    created_at TEXT DEFAULT NOW(),\n    updated_at TEXT DEFAULT NOW(),\n    FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"telegram_qr_sessions\" (\n    id TEXT PRIMARY KEY,\n    code TEXT NOT NULL UNIQUE,\n    user_id TEXT NOT NULL,\n    chat_id TEXT DEFAULT '',\n    bot_username TEXT DEFAULT '',\n    deep_link TEXT DEFAULT '',\n    status TEXT DEFAULT 'waiting',\n    created_at TEXT DEFAULT NOW(),\n    expires_at TEXT NOT NULL,\n    used_at TEXT,\n    FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"user_cv_profile\" (\n                user_id TEXT PRIMARY KEY,\n                skills TEXT NOT NULL DEFAULT '',\n                roles TEXT NOT NULL DEFAULT '',\n                experience TEXT NOT NULL DEFAULT '',\n                updated_at TEXT NOT NULL DEFAULT NOW(),\n                FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE\n            )",
    "CREATE TABLE \"user_job_preferences\" (\n                user_id TEXT PRIMARY KEY,\n                target_roles TEXT NOT NULL DEFAULT '[]',\n                locations TEXT NOT NULL DEFAULT '[]',\n                keywords TEXT NOT NULL DEFAULT '[]',\n                salary_min INTEGER,\n                work_modes TEXT NOT NULL DEFAULT '[]',\n                level TEXT,\n                must_have_skills TEXT NOT NULL DEFAULT '[]',\n                avoid_keywords TEXT NOT NULL DEFAULT '[]',\n                languages TEXT NOT NULL DEFAULT '[]',\n                updated_at TEXT NOT NULL DEFAULT NOW()\n            , compressed_week INTEGER DEFAULT 0, target_companies TEXT DEFAULT '[]')",
    "CREATE TABLE \"video_tasks\" (\n    id SERIAL PRIMARY KEY ,\n    user_id TEXT NOT NULL,\n    title TEXT NOT NULL,\n    source_type TEXT NOT NULL,\n    source_ref TEXT,\n    source_lang TEXT DEFAULT 'zh',\n    status TEXT NOT NULL DEFAULT 'queued',\n    progress TEXT,\n    video_file TEXT,\n    srt_file TEXT,\n    segments_count INTEGER,\n    duration DOUBLE PRECISION,\n    voice TEXT DEFAULT 'hoaimy',\n    funny INTEGER DEFAULT 0,\n    music INTEGER DEFAULT 0,\n    yt_desc TEXT,\n    yt_tags TEXT,\n    script TEXT,\n    pencil_scenes TEXT,\n    transitions TEXT,\n    error TEXT,\n    created_at BIGINT NOT NULL,\n    updated_at BIGINT NOT NULL, translate_provider TEXT DEFAULT 'groq', translate_model TEXT DEFAULT 'llama-3.3-70b-versatile',\n    FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"video_publish_log\" (\n    id SERIAL PRIMARY KEY ,\n    task_id INTEGER NOT NULL,\n    platform TEXT NOT NULL,\n    status TEXT NOT NULL DEFAULT 'pending',\n    url TEXT,\n    error TEXT,\n    created_at BIGINT NOT NULL,\n    FOREIGN KEY (task_id) REFERENCES video_tasks(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"wiki_entries\" (\n    id TEXT PRIMARY KEY,\n    user_id TEXT NOT NULL,\n    title TEXT NOT NULL,\n    summary TEXT DEFAULT '',\n    content TEXT NOT NULL,\n    topics TEXT DEFAULT '[]',\n    source TEXT DEFAULT 'chat',\n    created_at TEXT DEFAULT NOW(),\n    updated_at TEXT DEFAULT NOW(),\n    FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"wiki_embeddings\" (\n    entry_id TEXT PRIMARY KEY,\n    embedding TEXT NOT NULL,\n    updated_at TEXT DEFAULT NOW(),\n    FOREIGN KEY (entry_id) REFERENCES wiki_entries(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE \"workflows\" (\n                id TEXT PRIMARY KEY,\n                user_id TEXT NOT NULL,\n                name TEXT NOT NULL,\n                description TEXT NOT NULL DEFAULT '',\n                graph_json TEXT NOT NULL DEFAULT '{\"nodes\":[],\"edges\":[]}',\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                updated_at TEXT NOT NULL DEFAULT NOW()\n            )",
    "CREATE TABLE \"workflow_runs\" (\n                id TEXT PRIMARY KEY,\n                workflow_id TEXT NOT NULL,\n                user_id TEXT NOT NULL,\n                status TEXT NOT NULL DEFAULT 'running',\n                input_json TEXT NOT NULL DEFAULT '{}',\n                output_json TEXT,\n                error TEXT,\n                started_at TEXT NOT NULL DEFAULT NOW(),\n                finished_at TEXT,\n                FOREIGN KEY (workflow_id) REFERENCES workflows(id) ON DELETE CASCADE\n            )",
    "CREATE TABLE \"workflow_artifacts\" (\n                id TEXT PRIMARY KEY,\n                run_id TEXT NOT NULL,\n                workflow_id TEXT NOT NULL,\n                user_id TEXT NOT NULL,\n                node_id TEXT NOT NULL,\n                payload_json TEXT NOT NULL,\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                FOREIGN KEY (run_id) REFERENCES workflow_runs(id) ON DELETE CASCADE,\n                FOREIGN KEY (workflow_id) REFERENCES workflows(id) ON DELETE CASCADE\n            )",
    "CREATE TABLE \"workflow_edges\" (\n                id TEXT NOT NULL,\n                workflow_id TEXT NOT NULL,\n                from_node_id TEXT NOT NULL,\n                to_node_id TEXT NOT NULL,\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                PRIMARY KEY (id, workflow_id),\n                FOREIGN KEY (workflow_id) REFERENCES workflows(id) ON DELETE CASCADE\n            )",
    "CREATE TABLE \"workflow_nodes\" (\n                id TEXT NOT NULL,\n                workflow_id TEXT NOT NULL,\n                type TEXT NOT NULL,\n                title TEXT NOT NULL DEFAULT '',\n                config_json TEXT NOT NULL DEFAULT '{}',\n                position_x DOUBLE PRECISION NOT NULL DEFAULT 0,\n                position_y DOUBLE PRECISION NOT NULL DEFAULT 0,\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                PRIMARY KEY (id, workflow_id),\n                FOREIGN KEY (workflow_id) REFERENCES workflows(id) ON DELETE CASCADE\n            )",
    "CREATE TABLE \"workflow_run_steps\" (\n                id TEXT PRIMARY KEY,\n                run_id TEXT NOT NULL,\n                node_id TEXT NOT NULL,\n                node_type TEXT NOT NULL,\n                node_title TEXT NOT NULL,\n                status TEXT NOT NULL,\n                input_json TEXT,\n                output_json TEXT,\n                error TEXT,\n                started_at TEXT NOT NULL DEFAULT NOW(),\n                finished_at TEXT,\n                FOREIGN KEY (run_id) REFERENCES workflow_runs(id) ON DELETE CASCADE\n            )",
    "CREATE TABLE \"workflow_schedules\" (\n                workflow_id TEXT PRIMARY KEY,\n                user_id TEXT NOT NULL,\n                enabled INTEGER NOT NULL DEFAULT 0,\n                interval_seconds INTEGER NOT NULL DEFAULT 7200,\n                last_run_at TEXT,\n                next_run_at TEXT,\n                created_at TEXT NOT NULL DEFAULT NOW(),\n                updated_at TEXT NOT NULL DEFAULT NOW(),\n                FOREIGN KEY (workflow_id) REFERENCES workflows(id) ON DELETE CASCADE\n            )",
    "CREATE TABLE \"zalo_config\" (\n    id SERIAL PRIMARY KEY ,\n    user_id TEXT NOT NULL UNIQUE,\n    oa_id TEXT NOT NULL,\n    access_token TEXT NOT NULL,\n    refresh_token TEXT,\n    active INTEGER DEFAULT 1,\n    created_at TEXT DEFAULT NOW(),\n    updated_at TEXT DEFAULT NOW(),\n    FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE\n  )",
    "CREATE UNIQUE INDEX \"DayLog_userId_date_key\" ON \"DayLog\"(\"userId\", \"date\")",
    "CREATE INDEX \"EnglishLesson_userId_nextReviewAt_idx\" ON \"EnglishLesson\"(\"userId\", \"nextReviewAt\")",
    "CREATE INDEX \"Lesson_userId_nextReviewAt_idx\" ON \"Lesson\"(\"userId\", \"nextReviewAt\")",
    "CREATE INDEX \"MindmapNote_userId_date_idx\" ON \"MindmapNote\"(\"userId\", \"date\")",
    "CREATE INDEX \"MindmapNote_userId_topic_idx\" ON \"MindmapNote\"(\"userId\", \"topic\")",
    "CREATE UNIQUE INDEX \"MissionConfig_userId_key\" ON \"MissionConfig\"(\"userId\")",
    "CREATE UNIQUE INDEX \"PomodoroSession_userId_date_key\" ON \"PomodoroSession\"(\"userId\", \"date\")",
    "CREATE UNIQUE INDEX \"RoadmapItem_userId_id_key\" ON \"RoadmapItem\"(\"userId\", \"id\")",
    "CREATE UNIQUE INDEX \"User_email_key\" ON \"User\"(\"email\")",
    "CREATE INDEX idx_agent_todos_agent ON agent_todos(agent_id)",
    "CREATE INDEX idx_audit_action ON audit_log(action)",
    "CREATE INDEX idx_audit_actor ON audit_log(actor_id)",
    "CREATE INDEX idx_audit_created ON audit_log(created_at DESC)",
    "CREATE INDEX idx_auto_fetch_sources_due\n                ON auto_fetch_sources(enabled, next_run_at)",
    "CREATE INDEX idx_auto_fetch_sources_user\n                ON auto_fetch_sources(user_id, updated_at DESC)",
    "CREATE INDEX idx_backup_runs_map ON drive_backup_runs(map_id)",
    "CREATE INDEX idx_cached_jobs_source\n                ON cached_jobs(source)",
    "CREATE INDEX idx_cached_jobs_updated\n                ON cached_jobs(updated_at DESC)",
    "CREATE INDEX idx_chat_session_user ON chat_sessions(user_id)",
    "CREATE INDEX idx_coach_reminders_user\n                ON coach_reminders(user_id, created_at DESC)",
    "CREATE UNIQUE INDEX idx_custom_providers_user_name ON custom_providers(user_id, name)",
    "CREATE INDEX idx_cv_job_applications_profile_status ON cv_job_applications(profile_id, status, created_at DESC)",
    "CREATE UNIQUE INDEX idx_cv_job_applications_user_url ON cv_job_applications(user_id, job_url)",
    "CREATE INDEX idx_cv_job_searches_profile ON cv_job_searches(profile_id, created_at DESC)",
    "CREATE INDEX idx_cv_match_user_score\n                ON cv_match_scores(user_id, match_score DESC)",
    "CREATE INDEX idx_cv_profiles_user ON cv_profiles(user_id)",
    "CREATE INDEX idx_devices_user_last_active\n                ON devices(user_id, last_active DESC)",
    "CREATE INDEX idx_english_items_user_type\n                ON english_items(user_id, type, created_at ASC)",
    "CREATE INDEX idx_entertainment_captions_user_updated\n                ON entertainment_video_captions(user_id, updated_at DESC)",
    "CREATE INDEX idx_entertainment_video_categories_user_order\n                ON entertainment_video_categories(user_id, sort_order, label)",
    "CREATE INDEX idx_entertainment_videos_user_updated\n                ON entertainment_videos(user_id, updated_at DESC)",
    "CREATE INDEX idx_evolution_user_status\n                ON self_evolution_events(user_id, status, created_at DESC)",
    "CREATE INDEX idx_evolution_user_type\n                ON self_evolution_events(user_id, event_type, created_at DESC)",
    "CREATE INDEX idx_feedback_message\n                ON message_feedback(user_id, message_id)",
    "CREATE INDEX idx_goal_tasks_goal_status\n                ON goal_tasks(goal_id, status, priority ASC)",
    "CREATE INDEX idx_goals_user_status\n                ON agent_goals(user_id, status, updated_at DESC)",
    "CREATE INDEX idx_google_keepalive_delivery_month\n                    ON google_email_keepalive_deliveries(user_id, month_key, status)",
    "CREATE INDEX idx_learn_code_user\n                ON learn_code_sessions(user_id, created_at DESC)",
    "CREATE INDEX idx_learn_lessons_user_track\n                ON learn_lessons(user_id, track, created_at ASC)",
    "CREATE INDEX idx_learn_logs_user_date\n                ON learn_day_logs(user_id, date DESC)",
    "CREATE INDEX idx_learn_pomodoro_user\n                ON learn_pomodoro(user_id, date DESC)",
    "CREATE INDEX idx_learn_reports_user\n                ON learn_ai_reports(user_id, created_at DESC)",
    "CREATE INDEX idx_messages_session ON messages(session_id)",
    "CREATE INDEX idx_omni_agent_auto_reply_enabled\n                ON omni_agent_auto_reply(user_id, conversation_id, enabled)",
    "CREATE INDEX idx_omni_channels_user ON omni_channels(user_id)",
    "CREATE UNIQUE INDEX idx_omni_channels_user_platform ON omni_channels(user_id, platform)",
    "CREATE INDEX idx_omni_contact_platform ON omni_contacts(platform)",
    "CREATE INDEX idx_omni_contact_user ON omni_contacts(user_id)",
    "CREATE INDEX idx_omni_contacts_user_platform_external\n                ON omni_contacts(user_id, platform, external_id)",
    "CREATE INDEX idx_omni_conv_lastmsg ON omni_conversations(last_message_at DESC)",
    "CREATE INDEX idx_omni_conv_pinned ON omni_conversations(pinned)",
    "CREATE INDEX idx_omni_conv_platform ON omni_conversations(platform)",
    "CREATE INDEX idx_omni_conv_user ON omni_conversations(user_id)",
    "CREATE INDEX idx_omni_conversations_user_platform_external\n                ON omni_conversations(user_id, platform, external_id)",
    "CREATE INDEX idx_omni_messages_conversation_external\n                ON omni_messages(conversation_id, external_id)",
    "CREATE INDEX idx_omni_msg_conv ON omni_messages(conversation_id)",
    "CREATE INDEX idx_omni_msg_created ON omni_messages(created_at ASC)",
    "CREATE INDEX idx_session_todos_session ON session_todos(session_id, created_at ASC)",
    "CREATE INDEX idx_sessions_user_device ON sessions(user_id, device_id)",
    "CREATE INDEX idx_stories_slug\n                ON stories(slug)",
    "CREATE INDEX idx_stories_updated\n                ON stories(updated_at DESC)",
    "CREATE INDEX idx_story_chapters_story_slug\n                ON story_chapters(story_slug, chapter_number ASC)",
    "CREATE INDEX idx_task_results_iteration ON task_results(task_id, iteration)",
    "CREATE INDEX idx_task_results_task ON task_results(task_id)",
    "CREATE INDEX idx_tasks_state ON tasks(state)",
    "CREATE INDEX idx_tasks_user ON tasks(user_id)",
    "CREATE UNIQUE INDEX idx_telegram_chat_links_bot_chat\n    ON telegram_chat_links(bot_username, chat_id)",
    "CREATE INDEX idx_video_tasks_user ON video_tasks(user_id)",
    "CREATE INDEX idx_video_tasks_user_status\n                ON video_tasks(user_id, status, updated_at DESC)",
    "CREATE INDEX idx_wiki_updated ON wiki_entries(updated_at DESC)",
    "CREATE INDEX idx_wiki_user ON wiki_entries(user_id)",
    "CREATE INDEX idx_workflow_artifacts_run_created\n                ON workflow_artifacts(run_id, created_at ASC)",
    "CREATE INDEX idx_workflow_edges_workflow\n                ON workflow_edges(workflow_id)",
    "CREATE INDEX idx_workflow_nodes_workflow\n                ON workflow_nodes(workflow_id)",
    "CREATE INDEX idx_workflow_runs_workflow_started\n                ON workflow_runs(workflow_id, started_at DESC)",
    "CREATE INDEX idx_workflow_schedules_due\n                ON workflow_schedules(enabled, next_run_at)",
    "CREATE INDEX idx_workflow_steps_run_started\n                ON workflow_run_steps(run_id, started_at ASC)",
    "CREATE INDEX idx_workflows_user_updated\n                ON workflows(user_id, updated_at DESC)",
    "CREATE TABLE personal_note_categories (\n    id SERIAL PRIMARY KEY,\n    user_id TEXT NOT NULL,\n    name TEXT NOT NULL,\n    created_at TEXT DEFAULT NOW(),\n    UNIQUE(user_id, name),\n    FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE personal_notes (\n    id SERIAL PRIMARY KEY,\n    user_id TEXT NOT NULL,\n    title TEXT NOT NULL DEFAULT '',\n    content TEXT NOT NULL DEFAULT '',\n    category_id INTEGER,\n    pinned INTEGER NOT NULL DEFAULT 0,\n    created_at TEXT DEFAULT NOW(),\n    updated_at TEXT DEFAULT NOW(),\n    FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE,\n    FOREIGN KEY (category_id) REFERENCES personal_note_categories(id) ON DELETE SET NULL\n  )",
    "CREATE TABLE personal_todo_tasks (\n    id TEXT PRIMARY KEY,\n    user_id TEXT NOT NULL,\n    text TEXT NOT NULL,\n    status TEXT DEFAULT 'pending',\n    category TEXT DEFAULT 'work',\n    priority TEXT DEFAULT 'medium',\n    due_date TEXT,\n    assignee TEXT DEFAULT '',\n    created_at TEXT DEFAULT NOW(),\n    updated_at TEXT DEFAULT NOW(),\n    FOREIGN KEY (user_id) REFERENCES hagent_users(id) ON DELETE CASCADE\n  )",
    "CREATE TABLE personal_todo_subtasks (\n    id TEXT PRIMARY KEY,\n    task_id TEXT NOT NULL,\n    text TEXT NOT NULL,\n    completed INTEGER DEFAULT 0,\n    FOREIGN KEY (task_id) REFERENCES personal_todo_tasks(id) ON DELETE CASCADE\n  )",
    "CREATE INDEX idx_personal_notes_user ON personal_notes(user_id, updated_at DESC)",
    "CREATE INDEX idx_personal_todo_tasks_user ON personal_todo_tasks(user_id, created_at DESC)",
]
