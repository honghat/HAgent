{
  "name": "hagent",
  "version": "2.0.0",
  "private": true,
  "description": "AI-powered autonomous agent with backend loops, tool orchestration, and long-running task support",
  "type": "module",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "npm run start --workspace backend",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "build": "npm run build --workspace frontend",
    "test": "npm run build --workspace frontend && node --check backend/src/index.js"
  },
  "keywords": [
    "ai",
    "agent",
    "claude",
    "assistant",
    "telegram",
    "long-running-tasks",
    "tool-calling",
    "cron-scheduler"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "mammoth": "^1.12.0",
    "pdf-parse": "^2.4.5"
  }
}
